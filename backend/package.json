{
  "name": "kakei-backend",
  "version": "1.0.0",
  "description": "家計管理アプリ Lambda バックエンド",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/**/*.ts --bundle --platform=node --target=node20 --outdir=dist --format=esm",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/client-s3": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.126",
    "@types/node": "^20.10.6",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}