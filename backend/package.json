{
  "name": "kakei-backend",
  "version": "1.0.0",
  "description": "家計管理アプリ Lambda バックエンド",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/handlers/transactions.ts --bundle --platform=node --target=node20 --outfile=dist/handlers/transactions.js --format=cjs --external:@aws-sdk/* --external:uuid && esbuild src/handlers/categories.ts --bundle --platform=node --target=node20 --outfile=dist/handlers/categories.js --format=cjs --external:@aws-sdk/* --external:uuid && esbuild src/handlers/csv-import.ts --bundle --platform=node --target=node20 --outfile=dist/handlers/csv-import.js --format=cjs --external:@aws-sdk/* --external:uuid",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.500.0",
    "@aws-sdk/client-s3": "^3.500.0",
    "@aws-sdk/lib-dynamodb": "^3.500.0",
    "@aws-sdk/s3-request-presigner": "^3.1053.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.126",
    "@types/node": "^20.10.6",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
